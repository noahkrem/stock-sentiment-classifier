# Models

This directory contains generated model artifacts for the stock sentiment classifier project.

Contents are produced by the training pipeline; do not edit them manually.
For generation instructions and workflow details, refer to the root `README.md`.

Typical files:
- `model.pkl`: serialized trained model used for inference.
- `metrics.json`: evaluation metrics produced after model training.
